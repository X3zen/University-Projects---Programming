#include <stdio.h>

int main (){
    int counter = 1;

    while (counter <= 5) {      //ciclo con contatore
        printf("%d", counter);
        counter++;              //incremento del contatore
    }
    puts("");
return 0;
}